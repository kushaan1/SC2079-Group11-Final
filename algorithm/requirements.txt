# Runtime dependencies for the algorithm/ pathfinding service.
#
# Install into a Python 3.11+ virtualenv:
#     python3.11 -m venv .venv && ./.venv/bin/pip install -r requirements.txt
#
# Deliberately closed set: numpy, pydantic,
# flask-openapi3, flask-cors, plus pytest for tests. Do not add to it without amending the plan.
#
# Lower bounds, not pins. The floors are the majors these modules are written against —
# pydantic 2 (v1 has a different validator API and would not import), Flask 3. The versions
# actually exercised on 2026-08-27, on CPython 3.11.15, were:
#     numpy 2.4.6, pydantic 2.13.4, flask-openapi3 4.3.2, flask-cors 6.0.5, Flask 3.1.3
# If a teammate hits a version-specific break, pin here rather than editing a module.

numpy>=1.26
pydantic>=2.0,<3
flask-openapi3[swagger]>=4.0
flask-cors>=4.0

# Tests. Harmless to install now; nothing in the service imports it.
pytest>=8.0
